# rustReverse

Using the Rust programming language, write a function **rustReverse** that is the equivalent of **reverse** in q.

## Embedding
Rust is part of the c family, and can be compiled with the kx c header and the .so can be embedded with `2:` just like a c .so.